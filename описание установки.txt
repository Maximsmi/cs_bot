1. Установим необходимые библиотеки

2. Установка Psycopg2 и использование его API для доступа к базе данных PostgreSQL;
	
	$ pip install psycopg2
	
3. Debian по умолчанию включает PostgreSQL. 
   Чтобы установить PostgreSQL в Debian, используйте apt (или другую команду apt-get):
   
	$ 